[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bingfu"
version = "0.5.0"
authors = [
    { name = "SuXin (州哥)", email = "luke99810@example.com" },
]
description = "兵符 BingFu: 轻量级多智能体框架，支持将军战力体系 + 智能派兵 + LLM 接入"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "requests>=2.28",
    "openai>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "black>=23.0",
    "isort>=5.12",
    "mypy>=1.0",
]

[project.scripts]
bingfu = "bingfu.cli:main"

[project.urls]
Homepage = "https://github.com/luke99810/bingfu"
Repository = "https://github.com/luke99810/bingfu.git"
Issues = "https://github.com/luke99810/bingfu/issues"

[tool.setuptools.packages.find]
include = ["bingfu*"]

[tool.black]
line-length = 88
target-version = ['py310']

[tool.isort]
profile = "black"
multi_line_output = 3
